{
  "name": "friend-codex-router",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A local Codex-to-CCR gateway with one-time vision understanding and persistent image-summary caching.",
  "scripts": {
    "start": "node src/server.mjs",
    "test": "node --test",
    "check": "node --check src/server.mjs && node --check src/install-codex.mjs && node --test"
  },
  "engines": {
    "node": ">=22"
  }
}
